{
  "name": "lh-integration",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {},
  "engines": {
    "yarn": "^1.17.3",
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "yarn workspace lh-app start",
    "build": "yarn workspace lh-app build",
    "test": "yarn workspace lh-app test",
    "eject": "yarn workspace lh-app eject",
    "prod": "yarn workspace lh-app prod"
  }
}
